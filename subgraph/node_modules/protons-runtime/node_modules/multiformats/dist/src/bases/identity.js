import { coerce, fromString, toString } from "../bytes.js";
import { from } from "./base.js";
export const identity = from({
    prefix: '\x00',
    name: 'identity',
    encode: (buf) => toString(coerce(buf)),
    decode: (str) => coerce(fromString(str))
});
//# sourceMappingURL=identity.js.map